<!-- views/index.ejs -->
<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8"/>
  <title>키워드 검색</title>
  <style>
    body { font-family: sans-serif; margin: 40px; background-color: #fafafa; }
    .container { width: 400px; margin: 0 auto; text-align: center; }
    input[type="text"] {
      width: 200px; padding: 8px; border: 1px solid #ccc; border-radius: 4px;
    }
    button {
      padding: 8px 16px; margin-left: 8px; background: #4CAF50; color: #fff;
      border: none; border-radius: 4px; cursor: pointer; font-weight: bold;
    }
    button:hover { background: #45a049; }
    .error { color: red; margin-top: 10px; }
  </style>
</head>
<body>
  <div class="container">
    <h1>키워드 검색</h1>
    <form action="/keyword" method="POST">
      <input type="text" name="keyword" placeholder="키워드를 입력하세요" />
      <button type="submit">검색</button>
    </form>
    <% if (error) { %>
      <div class="error"><%= error %></div>
    <% } %>
  </div>
</body>
</html>
